import ProfileInformation from "./ProfileInformation";

const Configurations = ({ configs }) => {
  return (
    <div>
      <ProfileInformation configs={configs} />
    </div>
  );
};

export default Configurations;
